// import { selectResourcesContainerDomain } from '../selectors';

describe('selectResourcesContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
