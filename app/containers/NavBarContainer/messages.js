/*
 * NavBarContainer Messages
 *
 * This contains all the text for the NavBarContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NavBarContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NavBarContainer container!',
  },
});
