// import { selectAmenityContainerDomain } from '../selectors';

describe('selectAmenityContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
