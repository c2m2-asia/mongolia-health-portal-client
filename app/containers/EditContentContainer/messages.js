/*
 * EditContentContainer Messages
 *
 * This contains all the text for the EditContentContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EditContentContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditContentContainer container!',
  },
});
