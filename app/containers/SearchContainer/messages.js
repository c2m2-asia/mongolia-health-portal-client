/*
 * SearchContainer Messages
 *
 * This contains all the text for the SearchContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SearchContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SearchContainer container!',
  },
});
