/*
 * MobileResourcesContainer Messages
 *
 * This contains all the text for the MobileResourcesContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MobileResourcesContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MobileResourcesContainer container!',
  },
});
