/*
 * AdminLoginView Messages
 *
 * This contains all the text for the AdminLoginView component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AdminLoginView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminLoginView component!',
  },
});
